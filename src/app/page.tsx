import ThemeToggle from "@/Components/UI/Buttons/ThemeToggle";

const HomePage = () => {
  return (
    <main>
      <div className="min-h-screen flex justify-center items-center bg-primary text-secondary">
        Hello World
      </div>
    </main>
  );
};

export default HomePage;

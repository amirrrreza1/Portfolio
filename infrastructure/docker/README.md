# Docker assets

Multi-stage Dockerfiles, Compose configuration, health checks, and entrypoints will be added in the Docker implementation phase. The required service topology and hardening controls are specified in `docs/DOCKER.md`.

services:
  web:
    # The VPS already runs a host-level Caddy instance. Keep the application
    # private and let that trusted edge proxy to this loopback-only listener.
    ports:
      - "127.0.0.1:${PORTFOLIO_HTTP_PORT:-3010}:3000"

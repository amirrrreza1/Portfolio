# Prisma schema boundary

The implementation phase will add `schema.prisma`, migrations, and a deterministic JSON-to-PostgreSQL seed. The approved model is documented in `docs/DATA_MODEL.md`; `DATABASE_URL` is the only application connection input.

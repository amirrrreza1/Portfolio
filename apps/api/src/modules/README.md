# API modules

The feature implementation phase will add `auth`, `admin`, `blog`, `content`, `contact`, `media`, and shared `common` modules beside the existing health probe. Module boundaries and endpoint ownership are defined in `docs/ARCHITECTURE.md` and `docs/API_SPEC.md`.

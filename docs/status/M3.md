# M3 — Git content-store proof

Status: **In progress**  
Snapshot: **2026-08-16**  
Depends on: M1 and M2  
Roadmap: [M3 definition and exit gate](../ROADMAP.md#m3--git-content-store-proof)

## Outcome so far

The content-store package contains the core secure transport, reconciliation, and durable recovery primitives, and they are now wired into a running system: a PostgreSQL job queue, a dedicated worker process, an enqueue-only webhook endpoint, and readiness reporting that degrades instead of failing. What remains is everything that needs a real GitHub App — a protected branch, a bilingual direct push, and the cross-system failure drills.

## Delivered

- GitHub App installation-token exchange/cache and server-only runtime validation.
- Dedicated `content` branch and `content/` prefix enforcement with normalized safe paths.
- Blob-SHA optimistic concurrency and safe fixed commit metadata primitives.
- Signed webhook verification, replay-claim persistence, and trigger-only webhook handling.
- Git-tree reconciliation through the production Markdown renderer.
- PostgreSQL apply ledger/outbox schema and adapter for idempotent recovery.
- Unit/boundary coverage for traversal, unsafe writes, configuration, webhook/reconciliation behavior, and database boundaries.
- A `content_jobs` queue claimed with `FOR UPDATE SKIP LOCKED`, with leases, capped exponential backoff, attempt budgets, and dead-letter rows that are kept rather than deleted.
- Per-post ordering and burst collapsing enforced by partial unique indexes — one CLAIMED job per lock key, one PENDING job per dedupe key — so two workers racing produce a unique violation the loser retries rather than two interleaved handlers.
- A dedicated worker process built from the API workspace with `sync` and `scheduler` modes, each behind its advisory lock, waiting rather than exiting when another process holds it. HTTP API replicas run no timers.
- Lease recovery for a worker killed mid-job, plus a sweep that dead-letters a lapsed claim with no attempts left — otherwise that row is invisible to the claim query and never reports as failed.
- `POST /content/webhook`: verifies the signature over the unmodified request bytes, rejects a delivery with no id because replay cannot be detected without one, and enqueues a reconciliation carrying none of the payload. A redelivery answers `200` so the sender stops retrying.
- `/health/live` and `/health/ready` as separate questions. Only an unreachable database is `503`; an unconfigured, misconfigured, backlogged, or failing content pipeline reports itself at `200` with queue depth, age, dead count, and lapsed leases.
- The API boots and serves every public route with no content store configured, and reports a half-set configuration as misconfigured rather than as absent.

## Evidence

- Implementation/tests: [`packages/content-store`](../../packages/content-store)
- Queue implementation: [`packages/database/src/content-jobs.ts`](../../packages/database/src/content-jobs.ts)
- Queue semantics proven against PostgreSQL under PGlite: [`packages/database/test/content-jobs.spec.ts`](../../packages/database/test/content-jobs.spec.ts) — 25 assertions covering claim exclusion, lock-key serialization, dedupe, lease recovery, backoff, dead-lettering, and metrics
- Worker process: [`apps/api/src/worker`](../../apps/api/src/worker)
- Webhook and health surfaces: [`apps/api/src/modules/content`](../../apps/api/src/modules/content), [`apps/api/src/modules/health`](../../apps/api/src/modules/health)
- Security/flow specification: [CONTENT_PIPELINE.md](../CONTENT_PIPELINE.md)
- Accepted decisions: ADR-011, ADR-012, and ADR-013 in [DECISIONS.md](../DECISIONS.md)
- Delivery commits include `416711b`, `f876ac2`, `8cf3c7c`, `7606c4f`, `5d5b2d5`, and `b534158`.

## Outstanding gate work

- Configure a real GitHub App and protected `content` branch with force-push/deletion prevention and secret scanning.
- ~~Integrate durable jobs/reconciliation into a dedicated API worker process.~~ Built 2026-08-16; unproven against a real repository.
- Apply the `20260816150000_content_jobs` migration and confirm `prisma migrate diff` reports no drift — it was hand-written without a database.
- Add one real article in English and Persian by direct push and reconcile it into PostgreSQL/render cache.
- Force Git-success/database-failure and invalidation-failure cases; prove convergence without duplicate revisions.
- Prove invalid/deleted content preserves the previous live render and reports drift.
- Prove Git outage blocks authoring only, never public reads or database-driven publication.

## Next actions

1. Apply the content-jobs migration and verify it against `prisma migrate diff`.
2. Configure the GitHub App and the protected test content branch, then run the worker in `sync` mode against it.
3. Run the valid, invalid, deleted, stale-SHA, outage, replay, and forced-partial-failure matrix.
4. ~~Deliver the invalidation outbox to the web app~~ built 2026-08-16; the drain runs beside the job loop under the same advisory lock, and proving it end to end belongs to M4's gate.

# M5 — Appearance and accessibility

Status: **In progress**  
Snapshot: **2026-08-14**  
Depends on: M4 shell/public read path  
Roadmap: [M5 definition and exit gate](../ROADMAP.md#m5--appearance-and-accessibility)

## Outcome so far

The server-resolved preference shell now consumes the locale-scoped owner allowlist from PostgreSQL through the strict public API, with an explicit legacy rollback source, bounded validated fallback, and a real localized HTTP `503` when no allowed value exists. The accessible settings UI, CSP pre-paint path, reduced-motion wiring, and component token work are present. Font delivery and the complete accessibility/cache/hydration verification matrix remain incomplete.

## Delivered

- Strict `portfolio_prefs` contract and allowlisted theme/font/size/motion registry.
- Cookie-resolved `data-theme`/`data-motion` attributes in the initial server response.
- Blog typography scoped to `.blog-reading-surface`.
- Reviewed nonced pre-paint path for `system` mode and public security headers.
- Accessible settings dialog for appearance, typography, motion, and language.
- Reduced-motion wiring across hero, particles, scramble text, typing text, smooth scroll, and related effects.
- Inline application styles removed; theme-token refactoring has started.
- Strict public appearance contract and API projection for enabled themes, locale-compatible registry fonts, size steps, defaults, and the motion-toggle flag.
- Root layout appearance source selected by `PORTFOLIO_DATA_SOURCE=database|legacy`; the former hard-coded root settings singleton and dialog option arrays are removed.
- Appearance cache keys are resource/locale scoped and cookie independent; public API and rendered HTML responses do not send `Vary: Cookie`.
- Live first-response proof covers both locale defaults and a Persian visitor cookie whose valid light/reduced/XL choices are honored while an incompatible font is corrected to the configured Vazir default.
- Live rollback and fresh cold-outage runs prove the legacy source is API-independent and unavailable database data does not synthesize owner settings.
- A pre-stream production route gate now proves appearance cold/expired failures return localized secure HTTP `503`, while healthy and in-window validated pages remain `200` and explicit legacy mode never reads the API.

## Evidence

- Preference contracts/tests: [`packages/contracts/src/appearance`](../../packages/contracts/src/appearance) and [`packages/contracts/test/appearance.spec.ts`](../../packages/contracts/test/appearance.spec.ts)
- Appearance components/context: [`apps/web/src/Contexts`](../../apps/web/src/Contexts) and [`apps/web/src/Components/Appearance`](../../apps/web/src/Components/Appearance)
- Public appearance endpoint/client: [`apps/api/src/modules/public/public-appearance.controller.ts`](../../apps/api/src/modules/public/public-appearance.controller.ts) and [`apps/web/src/server/portfolio-appearance.ts`](../../apps/web/src/server/portfolio-appearance.ts)
- Appearance endpoint/client tests: [`apps/api/test/public-appearance.spec.ts`](../../apps/api/test/public-appearance.spec.ts), [`apps/web/test/public-appearance-client.spec.ts`](../../apps/web/test/public-appearance-client.spec.ts), and [`apps/web/test/portfolio-appearance-source.spec.ts`](../../apps/web/test/portfolio-appearance-source.spec.ts)
- Live database/rollback/outage record: [`evidence/M4-appearance-read.md`](evidence/M4-appearance-read.md)
- Superseding HTTP status proof: [`evidence/M4-http-503.md`](evidence/M4-http-503.md)
- Theme specification/test list: [THEMING.md §9](../THEMING.md#9-test-plan)
- Delivery commits include `e12ec8c`, `1c47126`, `250e627`, `d929e92`, `706a2d5`, and `d5ffaa4`.

## Outstanding gate work

- Complete the semantic theme-token vocabulary and add CI enforcement against hard-coded component colours.
- Subset fonts, add `unicode-range`, retain only active faces, and scope optional font preload/download to blog routes.
- Implement the one-time legacy `localStorage` theme-to-cookie migration or explicitly supersede that requirement.
- Add WCAG AA contrast tests for every enabled theme and state.
- Complete the first-response/hydration, cookie-tampering, `system`, no-JavaScript, shared-cache, blog-scoping, font-download, CSP, and error-page matrix; focused allowlist, correction, cache-key, and live no-`Vary: Cookie` proof now exists.

## Next actions

1. Finish the token migration and enforcement rule.
2. Implement the font registry/subsetting/delivery boundary.
3. Build the complete THEMING §9 automated suite.
4. Run keyboard/screen-reader/manual no-JavaScript verification and record results.
